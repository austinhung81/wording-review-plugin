const React = require("react");

const styles = require("./Warning.css");

const Warning = () => (
    <div>This plugin requires you to select a text in the document. Please select a text.</div>
);

module.exports = Warning;